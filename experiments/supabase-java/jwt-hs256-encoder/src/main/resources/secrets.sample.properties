# Add the supabase JWT secret here (Project Settings > API > JWT Settings)
#
# Alternatively generate a random key for testing with:
#   `openssl rand -hex 64 |  xxd -r -p | base64 -w 0`
jwt.secret=<secret string>
